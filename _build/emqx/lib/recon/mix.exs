defmodule Recon.MixProject do
  use Mix.Project

  def project do
    [
      app: :recon,
      description: "Diagnostic tools for production use",
      version: "2.5.0",
      language: :erlang,
      deps: []
    ]
  end
end
