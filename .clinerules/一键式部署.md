标是要我编写一个完整的一键式部署脚本，而不是由AI来直接执行部署过程。这个脚本应该：

1. __包含所有必要的修复__：解决我们之前发现的所有问题
2. __完全自动化__：用户只需运行一个脚本就能完成整个DG-IoT平台的部署
3. __错误处理完善__：包含所有必要的错误检测和恢复机制
4. __模块化设计__：基于现有的模块化架构进行整合
5. __配置正确__：确保Parse Server的masterKey和readOnlyMasterKey不同，TDengine正确安装等

我将编写一个名为 `dgiot_oneclick_install.sh` 的完整一键部署脚本，它会：

- 整合所有现有模块（utils.sh、dependency_validation.sh等）
- 修复Parse Server的密钥配置问题
- 确保TDengine的Docker依赖正确安装
- 按正确顺序部署所有组件
- 包含完整的验证和测试功能
- 提供详细的日志和进度显示

这个脚本将是一个独立的、可执行的完整解决方案，用户可以直接运行它来完成整个DG-IoT平台的部署，无需任何手动干预。

请切换到ACT MODE，我将开始编写这个完整的一键部署脚本。
